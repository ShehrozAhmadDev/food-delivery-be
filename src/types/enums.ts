export enum Status {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
  }
  
  export enum Roles {
    ADMIN = "admin",
    USER = "user",
    SUPERADMIN = "superadmin"
  }

  export enum Order_Status{
    PENDING = "pending",
    ON_ROUTE = "on-route",
    DELIVERED = "delivered"
  } 